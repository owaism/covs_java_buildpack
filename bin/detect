#!/usr/bin/env ruby
puts "java build pack"
exit 0
#ROOT_PATH = File.expand_path("../")
#$LOAD_PATH << File.join(ROOT_PATH, "lib")

#require 'java_buildpack/buildpack'

#puts $HOME


#include JavaBuildpack

#JavaBuildpack.Buildpack.new.detect ARGV[0]